# CONTRIBUTING — 開発規約

作成日: 2026-07-04
対象: 本リポジトリで実装を行うすべての人間・AI エージェント

本書は「誰が実行しても同じ品質の成果物」を出すための運用規約である。仕様の正本は docs/（読み順は [docs/00-vision.md](docs/00-vision.md) 末尾）、実装コントラクトの正本は [docs/08-content-specs.md](docs/08-content-specs.md)。

## 1. 環境

- **Godot 4.3 stable 固定**（docs/03 冒頭）。バージョン変更は専用 Issue でのみ行う
- GDScript のみ。C# / GDExtension の導入は事前に Issue で合意する

## 2. コーディング規約

| 項目 | 規約 |
|---|---|
| ファイル / ディレクトリ名 | snake_case（`enemy_brain.gd`）。配置は docs/03 §2 に従う |
| クラス | 再利用されるクラスは `class_name` を PascalCase で宣言 |
| 型付け | 変数・引数・返り値すべて型注釈必須（`-> void` 含む）。`Variant` の使用は理由コメント必須 |
| シグナル名 | 過去形または名詞（`state_changed`, `noise_emitted`）。定義は EventBus（グローバル）か自クラス（ローカル）のみ |
| 定数・enum | 共通 enum は `src/core/enums.gd` に集約（docs/08 §10.0）。マジックナンバー禁止 — 数値は `data/tuning/` の Resource から引く（NFR-04） |
| ノード間参照 | シーンをまたぐ直接参照禁止。EventBus シグナル + グループ（`lights` / `enemies` / `civilians` / `player_detect_points`）を使う（docs/03 §3.1） |
| ノードパス | docs/08 §10.2 のツリー構造を変更しない。変更が必要なら先に 08 を修正する PR を出す |
| コメント | コードで表せない制約・理由のみ書く。「何をしているか」の逐語コメント禁止 |
| pure 関数 | ユニットテスト対象の数式・判定は `static func`（副作用なし）に分離する（docs/08 §10.1 の pure 指定に従う） |

## 3. Git / PR 運用

- ブランチ名: `feat/issue-<番号>-<slug>` / `fix/issue-<番号>-<slug>` / `docs/<slug>`
- task / process と PR は、独立した目的と完了条件を持つ機能単位にする。Issue と PR の一対一対応は必須にしない。PR タイトル・本文は英語とし、関連 Issue を明記する。受け入れ条件をすべて満たした Issue にだけ `Closes #<番号>` を付ける
- main への直接 push 禁止。必要な検証と CI が成功し、既存の対応必須指摘を解消した PR はマージへ進める。通常の追加レビュー、CodeRabbit の応答、人間の再承認を一律の待ち条件にしない
- 独立レビューは権限拡大、認証・secret、データ消失リスクの変更に対して一度行う。指摘修正後は影響範囲の検証と元の指摘の解消確認を行い、全面再レビューや再帰レビューを繰り返さない
- マージ直前に repository / PR / current head / CI を照合し、指定 head を条件に通常の GitHub merge を使う。GitHub が実際に要求する保護条件に従い、存在しない独自の承認基盤の完成を前提にしない
- コミットは意味単位で分割。仕様変更（docs/08 のコントラクト変更）は**同一 PR 内で** docs を先に更新するコミットを含める

## 4. Definition of Done（全 Issue 共通・PR 本文にチェックリストとして貼る）

- [ ] この PR が担当する受け入れ条件を満たし、関連 Issue の残作業を明記している
- [ ] docs/08-content-specs.md §10（API / シーンツリー / レイヤー / FSM）に準拠している
- [ ] pure 指定の関数に GUT ユニットテストがある
- [ ] 数値をハードコードしていない（data/tuning/ 経由）
- [ ] 仕様と実装が食い違った場合、実装側で黙って解決せず docs を修正するコミットを含めた
- [ ] 動作確認手順（起動して何をどう確認したか）を PR 本文に記載した
- [ ] 該当する品質ゲート（docs/10-quality-gates.md）の合否記録を残した

## 5. Just-in-Time 指示書ルール

- 各 Issue の**実行指示書**は `docs/instructions/M<マイルストーン>/issue-<番号>.md` に置く（テンプレ: [docs/instructions/TEMPLATE.md](docs/instructions/TEMPLATE.md)）。実装する機能と直接の依存範囲について、追加変更へ着手する前に作成・更新する
- 指示書には、起点となる現在の `main` commit、対象の受け入れ条件との対応、依存の実装状況、担当 path / symbol、API / scene / collision / FSM の互換性、変更手順、影響範囲に応じた検証、scope 外事項を記載する。M0 の 5 本を粒度の基準とし、別の担当者が再現できる具体性を保つ
- マイルストーン全 Issue の一括設計や累積レビュー完了を、個別機能の着手条件にしない。共有契約や依存が変わった場合は、影響する指示書と検証範囲を更新する
- ユーザーの最新の明示指示を優先し、Issue・承認済み設計・指示書へ整合させる。受け入れ条件を黙って緩和せず、既存の承認範囲内の差異は理由と対応を記録して修正する。未確定の要件選択や範囲外の要件・設計・権限変更が必要な場合に、人間へ判断を求める。G4 の画風・補筆・確定台詞変更に必要な PO 判断、G5 の PO 判定・外部テスターによる実測は個別の成果物条件として維持し、未取得のまま品質ゲート通過・Phase 完了を宣言しない。これらを無関係な通常 PR の再承認待ちにしない
- 通常作業の進行と担当分担はメインエージェントが管理する。ハーネスの利用方法は作業ごとのユーザー指示に従い、今回限りの免除を恒久的な方針変更として扱わない

### 部分実装済みマイルストーンの復旧

履歴の正確性を保ち、追加実装に必要な準備を機能単位で行う。

1. 指示書なしで完了した Issue について、過去に指示書や承認があったように作成・遡及日付で記録しない。既存の code、merged PR、test、CI は「遡及的ベースライン」と明記して後続設計の参考にする
2. open / reopened Issue の既存 commit と staged / unstaged / untracked 差分を確認し、既存作業と今回の追加変更を分けて記録する。既存内容を破棄せず、後から用意した指示書に従って作成されたものと読み替えない
3. 再開する機能について、現在の `main` と既存差分、必要な依存、受け入れ条件を照合し、将来向けの指示書を先に用意する。完了済み Issue の reopen では、corrective scope だけを追加実装の対象にする
4. 既存の検証結果は実行時点・対象・残課題を明記する。変更の影響がない結果は再利用し、影響する範囲を再検証する。未実行・失敗・不明の検証を成功として扱わない
5. 通常の復旧で追加レビューや人間の再承認を要求しない。必要なリスクレビューは §3、検証は §7 に従い、指摘対応と CI 成功後は機能単位の PR を進める。他の全 Issue 完遂やハーネス全体の完成を待たない
6. 目的、変更理由、既存作業との区別、検証結果、PR / commit、残作業を Vault の task record に簡潔に残す。過去の成果物を実装前の指示・承認や未実施の検証の証明に使わない

## 6. エピック分割ルール（ミッション制作 Issue）

ミッションのゲームプレイ実装 Issue（#60, #62, #64, #66, #68, #70, #72, #75）はエピックであり、**着手前に以下の 4 サブ Issue に分割してから実装する**（親 Issue はトラッキング用に残し、サブ Issue を紐付ける）:

1. `<ミッション名>: レイアウト & 経路` — 地形・層構造・マーカー配置・3 経路の通行確認（マップ図を docs/maps/ に作成してから着手。見本: [docs/maps/m02-yashiki.md](docs/maps/m02-yashiki.md)）
2. `<ミッション名>: 敵配置 & ルーチン` — 敵・民間人・標的の配置と巡回 / 周期ルーチン
3. `<ミッション名>: 固有ギミック` — そのミッション固有のシステム（花火・鐘・おびき出し等）
4. `<ミッション名>: 通し検証 & チューニング` — 07 付録チェックリスト全通過・基準時間実測・ゲート G3 判定

## 7. テスト

- ユニットテスト: `tests/unit/test_<対象>.gd`（GUT）。pure 関数・遷移表・集計ロジックが対象
- 実行: `./scripts/run_tests.sh`（headless）。CI と同一コマンド。full validation は統合した変更一式に対して一度行い、各コミットで機械的に繰り返さない
- 修正後は影響範囲の検証を行う。文書のみなど非影響を説明できる変更では既存の full validation を再利用し、リンク・手順・差分整合を確認する。新たな変更・失敗・未解決の懸念があれば必要な検証を追加する
- 統合スモーク: シーン読み込み・セーブ往復は `tests/integration/`
- 手動検証: デバッグオーバーレイ（Issue #18）のトグルで数値確認し、手順を PR に記録

## 8. アセット

- 追加するすべての外部アセットは `docs/asset-licenses.md` に 出典 / ライセンス / 改変有無 を記録してから import する
- 公式 IP（天誅）由来のアセット・名称は一切禁止（NFR-03）
- バイナリは Git LFS（.gitattributes 参照）
