# Campaign selection validation (#53)

Main-agent implementation and self-review, Godot4.3, 2026-09-08. No delegation or repeated agent polling.

The missing catalog first failed its regression, then10 ordered MissionDefinition slots, unlock boundaries and the completion receipt passed2 tests/66 assertions. The new board and title-background host API each failed before implementation, then passed focused coverage. Existing first-clear narrative rules are reused; replay rank updates never recount narrative totals.

M3–M10 are metadata slots until their separately tracked level issues deliver PackedScenes. Their preparation status disables departure even when unlocked. This is not evidence that all10 levels are playable. M1 aliases the existing tutorial content through m01.tres; practice remains outside the campaign. Existing M2 navigation regression now supplies the required M1 unlock fixture.

Original SVG poster studies and the spring teahouse background are authored for this repository. They are UI illustrations, not completion of the70 narrative illustrations tracked by #78. The spring title is selected from an actual m10 completion receipt, not an arbitrary unlocked index.

Full suite verified exit0,542 passing tests/4508 assertions and no script errors; Python3 tests pass. Exported macOS build tested1280×720 and640×360 without horizontal overflow, and switched to the spring title from the m10 receipt. Screenshots were visually inspected; small windows use vertical scrolling. The actual displayed M2 loadout was empty, so a failing metadata regression was added before matching the production ToolRig defaults (stone10/dart5/smoke2). No gameplay inventory change is introduced by that metadata correction.

A separate native regression resized the already-open board and failed with horizontal overflow. The host now listens to viewport size changes; the same native regression passes. Final SVG chapter styling uses parchment, rank colors and disabled mist. The M2 loadout regression passes2 tests/67 assertions. CSV rows use LF and lint remains clean.

Final re-exported macOS build: exit0, live-resize and spring-title smoke0 failures, no engine/script errors. Main-agent review checked locked departure, missing-level handling, exact mission IDs, repeated clears, UI resource privacy and resize ownership. Known development export/GUT-loader and shutdown warnings remain separately recorded.
